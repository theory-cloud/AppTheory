# Testkit Examples (No AWS)

These examples exercise the per-language testkits without AWS credentials or network calls.

- TypeScript: `node examples/testkit/ts.mjs`
- Python: `python3 examples/testkit/py.py`

Both scripts are designed to run as part of `make rubric`.

