# TypeScript/Node contract test runner

Runs the shared contract fixtures against the Node (TypeScript) runtime implementation.

## Run

From the repo root:

`node contract-tests/runners/ts/run.cjs`

