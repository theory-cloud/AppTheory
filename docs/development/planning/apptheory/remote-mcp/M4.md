# M4 — Contract tests + docs + examples (Remote MCP / Claude)

Status: Complete  
Last updated: 2026-02-23  
Owner: AppTheory

This file is the work order + completion record for milestone **M4** of the “Claude-first Remote MCP” effort in AppTheory.

## Delivered (contract tests)

- Claude-first contract coverage (Go tests):
  - OAuth challenge (`401` + `WWW-Authenticate` `resource_metadata=...`)
  - `/.well-known/oauth-protected-resource` document shape
  - MCP lifecycle (`initialize` + `notifications/initialized` → `202`)
  - Streaming tool call disconnect + resume via `GET /mcp` + `Last-Event-ID`

## Delivered (testkit)

- Streamable HTTP SSE helpers in `testkit/mcp`:
  - `Client.RawStream(...)` (POST SSE)
  - `Client.ResumeStream(...)` (GET SSE + `Last-Event-ID`)
  - `ReadSSEMessage(...)` parser

## Delivered (docs)

- New guides:
  - `docs/integrations/remote-mcp.md` (build + deploy pointers + testing)
  - `docs/integrations/remote-mcp-autheory.md` (Autheory OAuth AS requirements + AppTheory helpers)

## Delivered (examples)

- New Go examples:
  - `examples/mcp/tools-only`
  - `examples/mcp/tools-resources-prompts`
  - `examples/mcp/resumable-sse`
